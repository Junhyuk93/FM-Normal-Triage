services:
  mjkim1-fm-normal-triage:
    build: .
    volumes:
      - /mnt/home/mjkim1/node5.gpu/self-supervised-learning:/workspace
      - /mnt/dataset/Normal_Triage_mi2rl:/workspace/dataset
    command: sleep infinity
    restart: unless-stopped
    extends:
      file: docker-compose-gpu-v3.yaml
      service: gpu
    ports:
      - "7776:7776"
    shm_size: '64gb'